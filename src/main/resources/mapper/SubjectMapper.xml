<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.demo.MybatisApplication.repository.SubjectRepository" >

    <resultMap id="SubjectResultMap" type="com.demo.MybatisApplication.model.SubjectEntity">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <association property="teacher" column="teacher_id" javaType="com.demo.MybatisApplication.model.TeacherEntity" select="selectTeacherBySubjectId" />
    </resultMap>

    <resultMap id="TeacherResultMap" type="com.demo.MybatisApplication.model.TeacherEntity">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="age" column="age"/>
        <result property="email" column="email"/>
    </resultMap>

    <select id="findSubjectById" resultMap="SubjectResultMap" parameterType="java.lang.Long">
        select * from subject where id = #{id}
    </select>

    <!-- Get All Subjects -->
    <select id="findAllSubjects" resultMap="SubjectResultMap" resultType="com.demo.MybatisApplication.model.SubjectEntity">
        SELECT * FROM subject
    </select>

    <select id="selectTeacherBySubjectId" parameterType="java.lang.Long" resultMap="TeacherResultMap">
        select t.id, t.name, t.age, t.email from teacher t INNER JOIN subject s on t.id = s.teacher_id where s.id = #{id}
    </select>

    <!-- Insert student -->
    <insert id="saveSubjects" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        <foreach collection="list" item="subject" separator=";">
            INSERT INTO subject (name, teacher_id)
            VALUES (#{subject.name}, #{subject.teacher.id})
        </foreach>
    </insert>


</mapper>
